---
phase: 14-canvas-mcp-and-connections
plan: "02"
type: execute
wave: 1
depends_on: ["14-01"]
files_modified:
  - server/canvas-tab-store.ts
  - server/canvas-tab-sse.ts
  - server/index.ts
autonomous: true
requirements: [MCP-01, MCP-02, MCP-03, MCP-04, CANVASTAB-04]

must_haves:
  truths:
    - "POST /api/canvas/tabs creates a tab, returns {id, title}"
    - "PUT /api/canvas/tabs/:id updates html with theme wrapping applied to body-only content"
    - "POST /api/canvas/tabs/:id/lock sets locked=true; /unlock sets locked=false"
    - "DELETE /api/canvas/tabs/:id removes the tab; subsequent GET returns 404"
    - "Tabs are persisted to .slop/canvas-state.json and reloaded on server restart"
    - "GET /api/canvas/events SSE endpoint pushes full tab list on any mutation"
  artifacts:
    - path: "server/canvas-tab-store.ts"
      provides: "CanvasTab type + in-memory Map CRUD + atomic persistence"
      exports: ["CanvasTab", "createTab", "updateTab", "lockTab", "unlockTab", "closeTab", "getTab", "getAllTabs", "initCanvasStore"]
    - path: "server/canvas-tab-sse.ts"
      provides: "SSE fan-out for tab changes"
      exports: ["registerCanvasTabSseClient", "notifyCanvasTabsUpdated"]
    - path: "server/index.ts"
      provides: "REST + SSE routes for /api/canvas/*"
      contains: "/api/canvas/tabs"
  key_links:
    - from: "server/canvas-tab-store.ts"
      to: ".slop/canvas-state.json"
      via: "atomicWrite (tmp+rename pattern)"
      pattern: "atomicWrite"
    - from: "server/canvas-tab-store.ts"
      to: "server/canvas-tab-sse.ts"
      via: "notifyCanvasTabsUpdated call on every mutation"
      pattern: "notifyCanvasTabsUpdated"
    - from: "server/index.ts"
      to: "server/canvas-tab-store.ts"
      via: "REST route handlers"
      pattern: "createTab|updateTab|lockTab"
---

<objective>
Backend: canvas tab store, SSE broadcast, and REST API. This is the data layer every other plan depends on.

Purpose: MCP tools (plan 05) and MultiTabCanvasPanel (plan 04) both need the REST API to be live before they can function.
Output: server/canvas-tab-store.ts, server/canvas-tab-sse.ts, and 8 new routes in server/index.ts.
</objective>

<execution_context>
@/Users/rgv250cc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rgv250cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-canvas-mcp-and-connections/14-CONTEXT.md
@.planning/phases/14-canvas-mcp-and-connections/14-RESEARCH.md
@.planning/phases/14-canvas-mcp-and-connections/14-01-SUMMARY.md

<interfaces>
<!-- Existing patterns to mirror exactly. -->

From server/live-canvas.ts (atomic write pattern to copy verbatim):
```typescript
async function atomicWrite(filePath: string, content: string): Promise<void> {
  const tmp = filePath + '.tmp';
  await writeFile(tmp, content, 'utf-8');
  await rename(tmp, filePath);
}
```

From server/live-canvas-sse.ts (SSE fan-out pattern to copy, removing cwd scoping):
```typescript
// Key: clients Set, writableEnded check before write, delete stale entries
type Client = { res: Response };
const clients = new Set<Client>();
```

From tests/canvas-tab-store.test.ts (contracts implemented here must satisfy):
- createTab(title: string) → CanvasTab (with UUID id, locked:false, html:'')
- updateTab(id, html) → void — wraps body-only; passes through DOCTYPE
- lockTab(id, reason?) → void
- unlockTab(id) → void
- closeTab(id) → void
- getTab(id) → CanvasTab | undefined
- getAllTabs() → CanvasTab[]
- initCanvasStore(projectRoot: string) → Promise&lt;void&gt;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: canvas-tab-store.ts — CRUD, persistence, HTML wrapping</name>
  <files>server/canvas-tab-store.ts</files>
  <behavior>
    - createTab(title) uses crypto.randomUUID() for id; sets locked=false, html='', createdAt/updatedAt=Date.now()
    - updateTab(id, rawHtml) applies wrapHtml(rawHtml): if trimmed starts with "<!DOCTYPE" or "<html" pass through; else inject full template with CSS vars + canvas classes
    - lockTab(id, reason?) sets locked=true, lockReason=reason, updates updatedAt
    - unlockTab(id) sets locked=false, clears lockReason, updates updatedAt
    - closeTab(id) deletes from Map
    - getTab(id) returns tab or undefined
    - getAllTabs() returns Array.from(tabs.values())
    - initCanvasStore(projectRoot) reads .slop/canvas-state.json if exists; parses JSON array; populates Map
    - Every mutation calls persist() which calls notifyCanvasTabsUpdated() after atomicWrite
    - HTML template injected by wrapHtml must include: all CSS custom properties from theme.css (--bg, --surface, --accent etc.), canvas utility classes (.canvas-table, .canvas-progress, .canvas-card, .canvas-badge, .canvas-timeline), default body styles (background: var(--bg), color: var(--txt), font-family: monospace)
    - Soft tab limit: createTab returns null if tabs.size >= 20; caller checks and returns error
  </behavior>
  <action>
    Create server/canvas-tab-store.ts with the module-level `tabs` Map and `stateFilePath` variable.

    Export the CanvasTab interface:
    ```typescript
    export interface CanvasTab {
      id: string;
      title: string;
      html: string;
      locked: boolean;
      lockReason?: string;
      createdAt: number;
      updatedAt: number;
    }
    ```

    Implement wrapHtml(rawHtml: string): string — trimStart check before DOCTYPE detection (per pitfall 5 in RESEARCH.md).

    CSS template must include ALL CSS variables from client/theme.css as inline :root block. Canvas classes to inject:
    - .canvas-table: width:100%, border-collapse:collapse, var(--border) borders
    - .canvas-progress: height:8px, background:var(--surface-hi), child .bar with background:var(--accent)
    - .canvas-card: padding:12px, background:var(--surface), border:1px solid var(--border), border-radius:6px
    - .canvas-badge: display:inline-flex, padding:2px 6px, font-size:11px, border-radius:3px
    - .canvas-timeline: list of events with left border var(--accent)

    Copy atomicWrite from live-canvas.ts exactly.

    notifyCanvasTabsUpdated import uses .js extension: `import { notifyCanvasTabsUpdated } from './canvas-tab-sse.js'`

    initCanvasStore must handle missing file gracefully (catch block, not throw).
  </action>
  <verify>
    <automated>npx vitest run tests/canvas-tab-store.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>All canvas-tab-store tests pass GREEN; createTab/updateTab/lockTab/unlockTab/closeTab/getTab/getAllTabs all verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: canvas-tab-sse.ts + REST routes in server/index.ts</name>
  <files>server/canvas-tab-sse.ts, server/index.ts</files>
  <behavior>
    canvas-tab-sse.ts:
    - registerCanvasTabSseClient(req, res) sets SSE headers, adds to clients Set, sends initial state, cleans up on close
    - notifyCanvasTabsUpdated() iterates clients, checks writableEnded before each write, removes stale clients
    - No cwd scoping (per pitfall 6 in RESEARCH.md — canvas tabs are global, not per-project)

    server/index.ts new routes (add after existing canvas routes, around line 490):
    - GET /api/canvas/events → registerCanvasTabSseClient
    - GET /api/canvas/tabs → 200 {tabs: getAllTabs()}
    - POST /api/canvas/tabs → body {title: string} → createTab; if null (limit hit) → 422 {error: 'tab limit reached'}; else 201 {id, title, ...}
    - GET /api/canvas/tabs/:id → 200 tab or 404 {error: 'tab not found'}
    - PUT /api/canvas/tabs/:id → body {html: string} → updateTab; 404 if missing; 200 tab
    - DELETE /api/canvas/tabs/:id → closeTab; 404 if missing; 204
    - POST /api/canvas/tabs/:id/lock → body {reason?: string} → lockTab; 404 if missing; 200 ok
    - POST /api/canvas/tabs/:id/unlock → unlockTab; 404 if missing; 200 ok
  </behavior>
  <action>
    Create server/canvas-tab-sse.ts mirroring live-canvas-sse.ts. Remove the cwdNorm filtering — canvas tabs are global to the server instance. Clients Set holds {res: Response} only. On connect, immediately send: `data: ${JSON.stringify({ tabs: getAllTabs() })}\n\n`.

    In server/index.ts, add import at top:
    ```typescript
    import { initCanvasStore, createTab, updateTab, lockTab, unlockTab, closeTab, getTab, getAllTabs } from './canvas-tab-store.js';
    import { registerCanvasTabSseClient } from './canvas-tab-sse.js';
    ```

    Call initCanvasStore(cwd) in the server startup path (near the existing initLiveCanvas call — search for "initLiveCanvas" to find correct placement).

    Add all 8 routes. Each route that mutates must handle the 404 case: if getTab(id) is undefined before mutation, return 404. Use `express.Router()` or add directly to `app` — match the existing pattern in index.ts.

    Do NOT remove or modify existing /api/live-canvas/* routes (they remain as fallback).
  </action>
  <verify>
    <automated>npx vitest run tests/canvas-tab-store.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All canvas-tab-store tests GREEN; server/index.ts compiles without TypeScript errors (npx tsc --noEmit passes or tsx starts without errors)</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/canvas-tab-store.test.ts` — all pass
- `npx vitest run` — full suite green (no regressions)
- Manual: `curl -X POST http://localhost:3001/api/canvas/tabs -H "Content-Type: application/json" -d '{"title":"Test"}' | jq` returns `{id, title, locked:false}`
</verification>

<success_criteria>
canvas-tab-store.ts + canvas-tab-sse.ts created; 8 REST routes added to index.ts; all store unit tests GREEN; full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-canvas-mcp-and-connections/14-02-SUMMARY.md`
</output>
