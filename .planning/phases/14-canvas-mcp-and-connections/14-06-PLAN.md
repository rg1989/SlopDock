---
phase: 14-canvas-mcp-and-connections
plan: "06"
type: execute
wave: 3
depends_on: ["14-04", "14-05"]
files_modified:
  - .slop/CLAUDE.md
autonomous: false
requirements: [MCP-05]

must_haves:
  truths:
    - ".slop/CLAUDE.md contains canvas_* tool usage instructions for Claude"
    - "User has visually verified the canvas tab bar and MCP modal in browser"
    - "Full test suite is green"
  artifacts:
    - path: ".slop/CLAUDE.md"
      provides: "Canvas tool instructions for Claude CLI agents"
      contains: "canvas_open"
  key_links:
    - from: ".slop/CLAUDE.md"
      to: "server/canvas-mcp-stdio.js"
      via: "documentation reference"
      pattern: "canvas_"
---

<objective>
Final phase wrap-up: update .slop/CLAUDE.md with canvas tool instructions, then human visual verification of the full feature.

Purpose: Claude CLI reads .slop/CLAUDE.md as project context. Without the canvas instructions, Claude will not know to use these tools. Human verify confirms the whole UI works end-to-end.
Output: .slop/CLAUDE.md updated; human approval of canvas tab bar + MCP modal visual.
</objective>

<execution_context>
@/Users/rgv250cc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rgv250cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-canvas-mcp-and-connections/14-CONTEXT.md
@.planning/phases/14-canvas-mcp-and-connections/14-04-SUMMARY.md
@.planning/phases/14-canvas-mcp-and-connections/14-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .slop/CLAUDE.md with canvas tool instructions</name>
  <files>.slop/CLAUDE.md</files>
  <action>
    Read the existing .slop/CLAUDE.md file first. Append the following section (do NOT overwrite existing content):

    ```markdown
    ## Canvas Tools (MCP)

    SlopMop exposes a local MCP server at `server/canvas-mcp-stdio.js` with five canvas tools. Use these when output benefits from visual layout.

    ### When to use canvas tools

    Use canvas tools when output would benefit from visual layout: task progress, data tables, timelines, analysis summaries, comparison matrices, diagrams. Open a new tab for each distinct topic. Lock the tab while a task is running. Unlock when done.

    ### Tool reference

    | Tool | Signature | When to use |
    |------|-----------|-------------|
    | `canvas_open` | `canvas_open(title)` → `{canvas_id}` | Start of a task that produces visual output |
    | `canvas_update` | `canvas_update(canvas_id, html)` → ok | Each time content changes |
    | `canvas_lock` | `canvas_lock(canvas_id, reason?)` → ok | Before starting a multi-step task |
    | `canvas_unlock` | `canvas_unlock(canvas_id)` → ok | After task completes |
    | `canvas_close` | `canvas_close(canvas_id)` → ok | When the canvas is no longer needed |

    ### HTML format

    Write **body HTML only** — no DOCTYPE, no `<html>`, no `<head>`. The SlopMop theme is injected automatically:
    - Dark background (`#0d1117`), monospace font, orange accent (`#d4845a`)
    - Pre-built classes: `.canvas-table`, `.canvas-progress`, `.canvas-card`, `.canvas-badge`, `.canvas-timeline`

    Use `<!DOCTYPE html>` prefix **only** if you need to override the theme entirely.

    ### Example: progress tracker

    ```html
    <div class="canvas-card">
      <h2>Task Progress</h2>
      <div class="canvas-progress"><div class="bar" style="width:60%"></div></div>
      <p style="color:var(--txt-sub)">Step 3 of 5 complete</p>
    </div>
    ```

    ### Port configuration

    The MCP script connects to SlopMop on `localhost:3000` by default. If you change the port, set the `SLOPMOP_PORT` environment variable in the MCP server config in `~/.claude/settings.json`.
    ```

    The canvas tool section must appear in .slop/CLAUDE.md exactly as written in the CONTEXT.md locked decision. Preserve all existing content.
  </action>
  <verify>
    <automated>grep -c "canvas_open" /Users/rgv250cc/Documents/Projects/SlopMop/.slop/CLAUDE.md</automated>
  </verify>
  <done>.slop/CLAUDE.md contains canvas tool documentation; existing content preserved; grep for "canvas_open" returns at least 1</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 14 complete:
    - Multi-tab canvas panel with SSE-driven tab state (agent opens tabs via MCP, user closes them)
    - Locked tab protection (dimmed × button, force-close modal)
    - 5 MCP tools in server/canvas-mcp-stdio.js (canvas_open/update/lock/unlock/close)
    - MCP Connections modal (fp-mcp-btn in FolderPicker toolbar, lists registered servers)
    - .slop/CLAUDE.md updated with canvas tool instructions
  </what-built>
  <how-to-verify>
    1. Start the app: `npm run dev` and open http://localhost:5173

    2. **Canvas tab bar (no tabs):**
       - Canvas column should show "No canvas yet" in the panel area
       - The old "Canvas" label header should be gone (replaced by MultiTabCanvasPanel)

    3. **Canvas tab via curl (simulating MCP):**
       ```bash
       curl -s -X POST http://localhost:3001/api/canvas/tabs \
         -H "Content-Type: application/json" \
         -d '{"title":"Test Analysis"}' | jq
       ```
       - Canvas column should show a tab chip "Test Analysis" with × button
       - The tab chip should have accent underline (active state)

    4. **Canvas content update:**
       ```bash
       TAB_ID=$(curl -s -X POST http://localhost:3001/api/canvas/tabs \
         -H "Content-Type: application/json" \
         -d '{"title":"Progress Tracker"}' | jq -r .id)
       curl -s -X PUT http://localhost:3001/api/canvas/tabs/$TAB_ID \
         -H "Content-Type: application/json" \
         -d '{"html":"<div class=\"canvas-card\"><h2>Hello</h2><p>Canvas works!</p></div>"}' | jq
       ```
       - Canvas panel should show the styled card with dark background and monospace text

    5. **Lock and force-close:**
       ```bash
       curl -s -X POST http://localhost:3001/api/canvas/tabs/$TAB_ID/lock \
         -H "Content-Type: application/json" \
         -d '{"reason":"running analysis"}' | jq
       ```
       - The × button should appear dimmed on the locked tab
       - Clicking × should show a modal: "This canvas is locked: running analysis. Close anyway?"
       - Cancel should dismiss the modal; Force Close should remove the tab

    6. **MCP connections button:**
       - FolderPicker toolbar should have a new icon button to the right of the canvas toggle
       - Clicking it should open the MCP Connections modal
       - Modal should list any registered MCP servers (may be empty initially)
       - "Auto-register slopmop-canvas" button should be visible
       - Clicking it should register the server and refresh the list

    7. **Full test suite:** `npm test` — all tests green
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- .slop/CLAUDE.md contains canvas tool section
- Human approval received for visual/functional verification
- `npx vitest run` — all tests green
</verification>

<success_criteria>
.slop/CLAUDE.md updated; human approves canvas tabs, lock behavior, and MCP modal; full test suite green; phase 14 complete
</success_criteria>

<output>
After completion, create `.planning/phases/14-canvas-mcp-and-connections/14-06-SUMMARY.md`
</output>
