---
phase: 16-overlays-cleanup
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - client/components/Composer.tsx
  - tests/Composer.test.tsx
  - client/App.css
autonomous: true
requirements:
  - CLEAN-01

must_haves:
  truths:
    - "No file named Composer.tsx exists in client/components/"
    - "No file named Composer.test.tsx exists in tests/"
    - "No CSS classes .composer-bottom, .composer-area, .composer-input remain in App.css"
    - "No import of Composer exists anywhere in client/ or tests/"
    - "The vitest suite passes with no 'Cannot find module' errors for Composer"
  artifacts:
    - path: "client/App.css"
      provides: "Cleaned of all Composer CSS — only terminal-input-strip and new overlay classes remain"
      contains: "terminal-input-strip"
  key_links:
    - from: "client/App.css"
      to: "(no Composer CSS)"
      via: "deletion of .composer-bottom, .composer-area, .composer-input blocks"
      pattern: "terminal-input-strip"
---

<objective>
Delete Composer.tsx, Composer.test.tsx, and all Composer-specific CSS from App.css. Verify no stale imports remain anywhere. Run the full test suite to confirm clean deletion.

Purpose: CLEAN-01 — the old textarea Composer is fully superseded by TerminalInput. Keeping it creates confusion and vitest failures.

Output: Three deletions. Zero Composer references remaining. Full suite still green.

Note: This plan has NO depends_on — it can run in parallel with Plan 03 (AttachBar). Composer.tsx is not imported by any Plan 01–03 work, and its deletion does not affect those files.
</objective>

<execution_context>
@/Users/rgv250cc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rgv250cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-overlays-cleanup/16-RESEARCH.md

<interfaces>
<!-- CSS blocks to delete from client/App.css (from RESEARCH.md line ranges) -->

Lines 55–60: .composer-bottom block
```css
.composer-bottom {
  flex-shrink: 0;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}
```

Lines 70–79: .composer-area block
```css
.composer-area {
  padding: 8px 12px;
  background: var(--surface);
  border-top: 1px solid var(--border);
  flex: 1;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  min-height: 0;
}
```

Lines 92–98: .composer-input and :focus block
```css
.composer-input {
  border: 1px solid var(--border);
  border-radius: 6px;
  outline: none;
}
.composer-input:focus {
  border-color: var(--accent);
}
```

IMPORTANT: The line numbers above are from pre-execution App.css. Plans 01 and 03 add CSS to App.css. Re-read App.css before editing — use the actual current line numbers, not these estimates.

Also IMPORTANT: `composerRef` in App.tsx is NOT Composer-related — it holds a TerminalInputHandle ref. Do NOT delete it or rename it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Composer files and CSS blocks</name>
  <files>
    client/components/Composer.tsx,
    tests/Composer.test.tsx,
    client/App.css
  </files>
  <action>
**Step 1 — Verify nothing imports Composer:**
```bash
grep -rn "from.*Composer\|import.*Composer" /Users/rgv250cc/Documents/Projects/SlopMop/client/ /Users/rgv250cc/Documents/Projects/SlopMop/tests/
```
Expected: zero results (Phase 15 removed Composer from SessionPane already). If any imports remain, remove them first.

**Step 2 — Delete files:**
```bash
rm /Users/rgv250cc/Documents/Projects/SlopMop/client/components/Composer.tsx
rm /Users/rgv250cc/Documents/Projects/SlopMop/tests/Composer.test.tsx
```

**Step 3 — Remove CSS blocks from `client/App.css`:**

Read App.css first to find current line numbers (they may have shifted from Plans 01 and 03 additions). Then remove these three blocks entirely:
- The `.composer-bottom { ... }` block
- The `.composer-area { ... }` block
- The `.composer-input { ... }` and `.composer-input:focus { ... }` block

Use the Read tool to view App.css and identify the exact lines, then use the Edit tool to remove only those blocks. Do not touch any other CSS.

**Step 4 — Verify complete cleanup:**
```bash
grep -rn "Composer" /Users/rgv250cc/Documents/Projects/SlopMop/client/ /Users/rgv250cc/Documents/Projects/SlopMop/tests/
```
Expected: zero results (the word "Composer" should not appear in any file under client/ or tests/).

```bash
grep -n "composer-bottom\|composer-area\|composer-input" /Users/rgv250cc/Documents/Projects/SlopMop/client/App.css
```
Expected: zero results.

**Step 5 — Run full test suite:**
```bash
cd /Users/rgv250cc/Documents/Projects/SlopMop && npm test -- --run
```
Expected: all tests pass, no "Cannot find module" error for Composer.

**Edge case:** If any other file (App.tsx, another component) still references Composer, remove those references. The Phase 15 work replaced Composer in SessionPane with TerminalInput, so no active references should exist. The only reference might be App.tsx passing a `composerRef` prop — but that ref is now typed as `TerminalInputHandle`, not Composer-related. Do NOT remove `composerRef` from App.tsx.
  </action>
  <verify>
    <automated>cd /Users/rgv250cc/Documents/Projects/SlopMop && grep -rn "Composer" client/ tests/ 2>/dev/null | grep -v "composerRef" && echo "FAIL: Composer references remain" || echo "PASS: No Composer references" && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>Composer.tsx deleted, Composer.test.tsx deleted, 3 CSS blocks removed from App.css. grep returns zero Composer references (excluding composerRef variable). Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `ls /Users/rgv250cc/Documents/Projects/SlopMop/client/components/Composer.tsx` — "No such file"
2. `ls /Users/rgv250cc/Documents/Projects/SlopMop/tests/Composer.test.tsx` — "No such file"
3. `grep -n "composer-bottom\|composer-area\|composer-input" /Users/rgv250cc/Documents/Projects/SlopMop/client/App.css` — zero results
4. `grep -rn "from.*Composer\|import.*Composer" /Users/rgv250cc/Documents/Projects/SlopMop/client/ /Users/rgv250cc/Documents/Projects/SlopMop/tests/` — zero results
5. `npm test -- --run` — full suite passes
</verification>

<success_criteria>
- Composer.tsx does not exist
- Composer.test.tsx does not exist
- .composer-bottom, .composer-area, .composer-input CSS blocks are gone from App.css
- No Composer imports remain anywhere in client/ or tests/
- vitest full suite is green with no module-not-found errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-overlays-cleanup/16-04-SUMMARY.md`
</output>
