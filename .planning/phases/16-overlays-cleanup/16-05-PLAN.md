---
phase: 16-overlays-cleanup
plan: 05
type: execute
wave: 3
depends_on:
  - 16-01
  - 16-02
  - 16-03
  - 16-04
files_modified: []
autonomous: false
requirements:
  - ACTION-01
  - ACTION-02
  - SLASH-01
  - SLASH-02
  - ATTACH-01
  - ATTACH-02
  - CLEAN-01

must_haves:
  truths:
    - "Attach, Voice, and TTS buttons appear as a floating overlay and remain accessible while typing"
    - "The action bar sits at top-right — typing text and the cursor are fully visible below it"
    - "Typing / at empty input opens the slash menu popup; arrow keys navigate it; Enter selects"
    - "Attached files appear as chips above the input strip; clicking X removes each chip"
    - "No Composer textarea code exists anywhere — old component and CSS are fully deleted"
  artifacts:
    - path: "client/components/ActionBar.tsx"
      provides: "Floating action bar — verified visually in browser"
    - path: "client/components/TerminalInput.tsx"
      provides: "xterm slash interception — verified in real browser"
    - path: "client/components/SessionPane.tsx"
      provides: "All overlays wired — verified visually in browser"
  key_links:
    - from: "browser"
      to: "all 5 success criteria"
      via: "manual verification"
      pattern: "human-verify"
---

<objective>
Human verification of all Phase 16 success criteria in the browser. All automated tests have passed in Plans 01–04. This plan confirms the visual and interactive behaviors that cannot be tested in jsdom.

Purpose: Gate before marking phase complete. The 5 success criteria involve xterm canvas rendering, CSS absolute positioning, and real keyboard interception — all require a live browser.

Output: Human confirmation that all 5 phase success criteria are satisfied (or a list of issues to fix).
</objective>

<execution_context>
@/Users/rgv250cc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rgv250cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-overlays-cleanup/16-01-SUMMARY.md
@.planning/phases/16-overlays-cleanup/16-02-SUMMARY.md
@.planning/phases/16-overlays-cleanup/16-03-SUMMARY.md
@.planning/phases/16-overlays-cleanup/16-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run dev server and full test suite</name>
  <files></files>
  <action>
Before asking for human verification, confirm the automated baseline is clean:

```bash
cd /Users/rgv250cc/Documents/Projects/SlopMop && npm test -- --run
```

All tests must be green. If any fail, fix them before proceeding to the checkpoint.

Then start the dev server:
```bash
cd /Users/rgv250cc/Documents/Projects/SlopMop && npm run dev
```

Confirm the server starts on its normal port (typically http://localhost:5173). The dev server must be running for the human verify checkpoint.
  </action>
  <verify>
    <automated>cd /Users/rgv250cc/Documents/Projects/SlopMop && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>Full test suite green. Dev server started and accessible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All Phase 16 overlays are implemented and Composer is deleted:
- ActionBar (floating action bar with VoiceBar + attach button) at top-right of terminal input strip
- Slash command popup (opens on / keypress, navigated with arrow keys)
- AttachBar chip strip (floats above input when files are attached)
- Composer.tsx and Composer.test.tsx deleted; 3 CSS blocks removed from App.css
  </what-built>
  <how-to-verify>
Open http://localhost:5173 (or whatever port `npm run dev` reports). Start a Claude session.

**Criterion 1 — ACTION-01:** Attach, Voice, and TTS buttons appear as floating overlay on the input strip.
- Look at the 80px terminal input strip at the bottom of the session pane
- You should see small icon buttons (mic, speaker) floating in the top-right corner of the strip
- An attach icon (paperclip) should also be present

**Criterion 2 — ACTION-02:** Action bar does not cover the active input line.
- Type several characters in the terminal input
- The cursor and typed text should be fully visible — NOT hidden behind the action bar
- The action bar floats at the TOP of the 80px strip; the cursor lives at the BOTTOM

**Criterion 3 — SLASH-01 + SLASH-02:** Slash command popup works.
- Click in the terminal input to focus it
- Press / (forward slash) — a popup menu should appear ABOVE the input strip listing slash commands
- Press ArrowUp / ArrowDown — the highlighted row should move
- Press Enter — the selected command text should be injected into the terminal input
- Re-open the menu, then press Escape — the menu should close without injecting text

**Criterion 4 — ATTACH-01 + ATTACH-02:** Attachment chips work.
- Click the attach (paperclip) button in the action bar — a file picker should open
- Select a file — a chip should appear in a strip directly above the 80px input strip
- Click the X on the chip — the chip should disappear

**Criterion 5 — CLEAN-01:** No Composer code remains.
- Run in a separate terminal: `grep -rn "Composer" client/ tests/ | grep -v composerRef`
- Expected: zero results
  </how-to-verify>
  <resume-signal>
Type "approved" if all 5 criteria pass, or describe which criteria failed (e.g., "Criterion 2 fails — action bar covers typing area") so fixes can be planned.
  </resume-signal>
</task>

</tasks>

<verification>
Human sign-off on all 5 phase success criteria:
1. ACTION-01: Floating action bar visible with Attach/Voice/TTS buttons
2. ACTION-02: Cursor row visible while action bar is present
3. SLASH-01+02: Slash popup opens, navigates, and selects
4. ATTACH-01+02: Chip strip appears and dismisses correctly
5. CLEAN-01: grep returns zero Composer references
</verification>

<success_criteria>
User types "approved" confirming all 5 criteria pass in the live browser.
</success_criteria>

<output>
After completion, create `.planning/phases/16-overlays-cleanup/16-05-SUMMARY.md`
</output>
